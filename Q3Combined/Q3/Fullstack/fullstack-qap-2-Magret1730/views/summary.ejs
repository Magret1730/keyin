<%- include('partials/header') %>
<main class="main-content">
    <section class="hero">
        <h2 class="hero__title">😇 Your Mood Summary 😋</h2>
        <p class="hero__subtitle">Overview of your emotional journey</p>
    </section>
    <section class="card mood-card">
        <p class="card-positive">😊 Current Positive Streak: <strong><%= streak %> day(s)</strong></p>

        <div class="summary-section">
            <h3>Last 5 Mood Entries</h3>
            <% if (lastFive.length === 0) { %>
                <p>No entries yet!</p>
            <% } else { %>
                <ol>
                <% lastFive.forEach(entry => { %>
                    <li><%= entry.date %>: <%= entry.mood %></li>
                <% }) %>
                </ol>
            <% } %>
        </div>
    </section>
    <div class="button-group button-group--space">
        <button class="btn mood-btn">
            <a href="/">😊 Go Home</a>
        </button>
        <button class="btn mood-btn">
            <a href="/mood">🤩 Add Mood</a>
        </button>
    </div>
</main>
</body>

</html>